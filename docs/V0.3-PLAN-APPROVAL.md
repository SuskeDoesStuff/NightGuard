Plan approved. Proceed, with four corrections.

1. Discrepancy 1 — accepted as written. My error on both counts. Use {0.5, 1.0} for the
   hard zero, state the bound as 1.4 s quantised, and write both the reachability
   constraint and the 1.33 -> 1.40 quantisation into §8.4.

2. Discrepancies 2 and 3 — diagnosis correct, figures not reproduced, and there is a
   third ambiguity underneath. Checking eight conventions:

     phases t=5, kill +2:  0.6148 inclusive / 0.6367 strict
     phases t=5, kill +0:  0.5443 / 0.5880
     phases t=0, kill +2:  0.4389 / 0.4546
     phases t=0, kill +0:  0.3847 / 0.4140

   Your 0.4919 and 0.3511 are not in that grid. Show your working for both so we can
   find the divergence, but do not block on it: they describe rejected readings.

   What does matter: whether a kill roll landing exactly on the budget boundary counts
   moves the RECOMMENDED figure from 0.6148 to 0.6367. At n=10,000 that is 4.5 sigma,
   so a correct implementation fails criterion 3 on convention alone and the failure
   looks like a blackout bug. Resolve it as INCLUSIVE, and write the boundary
   convention into §3.11 alongside the roll offsets. Record as a §10 row.

3. Discrepancy 4 — the sign is backwards. Measured, WARDEN only, stage lock off,
   1a patched, 400 seeds, STAGE -> E_CORNER:

     AI   measured    naive   naive+phase   reached corner
      1     334.73   377.00        384.55   363/400
      2     210.03   217.67        225.22   400/400
      4     122.75   125.50        133.05   400/400
      7      60.80    68.14         75.69   400/400
      9      35.17    41.89         49.44   400/400
     10      30.40    30.20         30.20   400/400

   Measured is below naive everywhere, so a phase penalty moves the model away from
   the truth. The countdown consumes part of an inter-firing interval, so the next
   opportunity arrives sooner than a full 3.02 s after a move. Correcting with
   delta(C) = 3.02*ceil(C/3.02) - C lands within ~5% at every level. There is no phase
   effect at AI 10 at all, since there is no countdown.

   More important: at AI 1 only 363/400 reach the corner before dawn. The latency
   distribution is censored by night length, so mean-based comparison at low AI is
   biased downward and §8.3 will fail for reasons unrelated to fidelity. Decide and
   record how you handle it — condition on completion, run a synthetic extended night,
   or compare distributions — before deriving the targets.

4. A6 — agreed, no §10 row moving the curriculum. But record the second finding too:
   rhythm at 0.998 on night 3 leaves almost no headroom for an agent to learn against.
   That is a better argument for moving the curriculum start than the one in the prompt,
   and v1.1 should inherit the reasoning, not just the number.

Everything else stands. Start with Part 0.